<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>محول وضغط الصور</title>
    <style>
        :root { --primary: #0056b3; --white: #fff; --gray: #e9ecef; }
        body { font-family: 'Segoe UI', Tahoma, sans-serif; margin: 0; padding: 20px; background: #f4f7f6; }
        .tool-box { background: var(--white); padding: 2rem; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); border: 1px solid #eee; max-width: 550px; margin: 0 auto; text-align: center; }
        .tool-box h2 { color: var(--primary); border-bottom: 2px solid var(--gray); padding-bottom: 10px; margin-bottom: 20px; }
        input[type="file"] { width: 100%; padding: 1.5rem 1rem; border: 2px dashed #ccc; border-radius: 4px; box-sizing: border-box; cursor: pointer; margin-bottom: 1rem; background: #fafafa; }
        select, input[type="range"] { width: 100%; padding: 10px; margin-bottom: 1rem; }
        .flex-group { display: flex; justify-content: space-between; gap: 10px; margin-bottom: 1rem; text-align: right; }
        .flex-group div { flex: 1; }
        button { background: var(--primary); color: var(--white); border: none; padding: 12px; border-radius: 5px; cursor: pointer; font-size: 1.1rem; width: 100%; font-weight: bold; }
        .result { margin-top: 15px; padding: 15px; background: var(--gray); border-radius: 5px; display: none; text-align: right; }
    </style>
</head>
<body>
    <div class="tool-box">
        <h2>محول ومضغط الصور الاحترافي</h2>
        <input type="file" id="imageInput" accept="image/*">
        
        <div class="flex-group">
            <div>
                <label>صيغة التحويل المرجوة:</label>
                <select id="formatSelect">
                    <option value="image/jpeg">JPEG (أفضل حجم)</option>
                    <option value="image/png">PNG (أعلى جودة)</option>
                    <option value="image/webp">WebP (حديث ومضغوط)</option>
                </select>
            </div>
            <div>
                <label>جودة الضغط (<span id="qualVal">80</span>%):</label>
                <input type="range" id="qualityRange" min="10" max="100" value="80" oninput="document.getElementById('qualVal').innerText=this.value">
            </div>
        </div>

        <button onclick="processImage()">معالجة وتحميل الصورة</button>
        <div id="imgResult" class="result"></div>
    </div>

    <script>
        function processImage() {
            const fileInput = document.getElementById('imageInput');
            const format = document.getElementById('formatSelect').value;
            const quality = document.getElementById('qualityRange').value / 100;
            const resBox = document.getElementById('imgResult');

            if (!fileInput.files[0]) return alert("الرجاء اختيار صورة أولاً.");
            
            const file = fileInput.files[0];
            const reader = new FileReader();
            
            reader.onload = function(event) {
                const img = new Image();
                img.onload = function() {
                    const canvas = document.createElement('canvas');
                    canvas.width = img.width;
                    canvas.height = img.height;
                    
                    const ctx = canvas.getContext('2d');
                    ctx.drawImage(img, 0, 0);
                    
                    canvas.toBlob(function(blob) {
                        const originalSize = (file.size / 1024).toFixed(1);
                        const newSize = (blob.size / 1024).toFixed(1);
                        
                        const url = URL.createObjectURL(blob);
                        const a = document.createElement('a');
                        a.href = url;
                        a.download = `smarttools_${Date.now()}.${format.split('/')[1]}`;
                        a.click();
                        
                        resBox.style.display = "block";
                        resBox.innerHTML = `
                            <strong>تمت المعالجة بنجاح!</strong><br>
                            الحجم الأصلي: ${originalSize} KB<br>
                            الحجم الجديد: <span style="color:green; font-weight:bold;">${newSize} KB</span>
                        `;
                    }, format, quality);
                }
                img.src = event.target.result;
            }
            reader.readAsDataURL(file);
        }
    </script>
</body>
</html>